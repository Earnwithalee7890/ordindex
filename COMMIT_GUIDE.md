# 🚀 How to Get 5 Commits for Ordindex

Follow this sequence to generate 5 high-quality commits.

**Prerequisite**:
Open your terminal in `f:\stacks febuarary\ordindex` and run:
`git init`

---

1.  **Commit 1**: Initial Project Structure
    - `git add Clarinet.toml`
    - `git commit -m "chore: initialize project with Clarinet config"`

2.  **Commit 2**: Add Registry Contract
    - `git add contracts/ordindex.clar`
    - `git commit -m "feat(contract): implement decentralized inscription registry"`

3.  **Commit 3**: Add Mock Indexer
    - `git add src/indexer.ts`
    - `git commit -m "feat(indexer): add typescript service for scanning bitcoin blocks"`

4.  **Commit 4**: Add Frontend
    - `git add index.html`
    - `git commit -m "feat(ui): create search dashboard for inscriptions"`

5.  **Commit 5**: Finalize Documentation
    - `git add .`
    - `git commit -m "docs: add readme and usage instructions"`

---

**Done!** Push to GitHub.
